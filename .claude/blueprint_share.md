# share feature

I want to create a share feature:
with this feature, it will be possible to share a file or folder. 
This will generate a link, which will give anyone with this hashed link access to view and download files from anyone's Explorer page.

The link can or cannot have an expiration.

This might require an API change, new database (to save the hashes), and app changes — both to generate the share link, as to view a shared link without the require of logging in.
The shared link should never give the API which it belongs.

## result
Create a `.claude/plan_share.md` file, which I will review before implementation.
Give me detailed steps on the implementation as well.

Take as much time as you need.
